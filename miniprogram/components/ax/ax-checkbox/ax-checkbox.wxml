<view class="_checkbox" style="--color:{{color}}" bind:tap="f_checkChange">
  <!-- <checkbox value="{{value}}" checked="{{checked}}" disabled="{{disabled}}" color="{{color}}"  /> -->
  <view class="_checkbox-icon {{checked?'checked':''}}" value="{{value}}"></view>
  <slot></slot>
</view>