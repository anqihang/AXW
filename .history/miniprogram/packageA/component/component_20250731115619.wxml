<view class="Component page">
    <nav-bar title="组件" backButton="{{false}}" homeButton="{{false}}" color="white" background="linear-gradient(45deg,#0081ff,#1cbbb4)"></nav-bar>
    <ax-tabs tabs="{{tabsList}}" center justify="space-evenly">
        <view slot="content" class="flex">
            <view class="w-full shrink-0">1</view>
            <view class="w-full shrink-0">2</view>
        </view>
    </ax-tabs>
</view>